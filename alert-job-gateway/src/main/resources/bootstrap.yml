eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false
server:
  port: 8015
spring:
  application:
    name: alert-job-gateway
  cloud:
    config:
      uri: http://config:8011
    loadbalancer:
      ribbon: false
  profiles:
    active: dev  