server.port=8017
spring.application.name=parser
spring.cloud.config.uri=http://config:8011
spring.cloud.loadbalancer.ribbon=false
eureka.client.registerWithEureka=false
eureka.client.fetchRegistry=false
cleanup.retention=90d
core.api.url=http://localhost:8018/api/orders

parser.schedule.sites.flru.initial-delay-seconds=300
parser.schedule.sites.flru.fixed-delay-seconds=300

parser.schedule.sites.kwork.initial-delay-seconds=600
parser.schedule.sites.kwork.fixed-delay-seconds=600

parser.schedule.sites.weblancer.initial-delay-seconds=300
parser.schedule.sites.weblancer.fixed-delay-seconds=300

parser.schedule.sites.freelancer.initial-delay-seconds=300
parser.schedule.sites.freelancer.fixed-delay-seconds=300

parser.schedule.sites.freelanceru.initial-delay-seconds=300
parser.schedule.sites.freelanceru.fixed-delay-seconds=300

parser.schedule.sites.freelancehunt.initial-delay-seconds=300
parser.schedule.sites.freelancehunt.fixed-delay-seconds=300

parser.schedule.sites.youdo.initial-delay-seconds=300
parser.schedule.sites.youdo.fixed-delay-seconds=300

parser.schedule.sites.truelancer.initial-delay-seconds=300
parser.schedule.sites.truelancer.fixed-delay-seconds=300

parser.schedule.sites.peopleperhour.initial-delay-seconds=300
parser.schedule.sites.peopleperhour.fixed-delay-seconds=300

parser.schedule.sites.workspace.initial-delay-seconds=300
parser.schedule.sites.workspace.fixed-delay-seconds=300

parser.schedule.sites.workana.initial-delay-seconds=300
parser.schedule.sites.workana.fixed-delay-seconds=300